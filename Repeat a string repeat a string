
function repeatStringNumTimes(str, num) {
  // repeat after me
  if(num <1){
    return "";
  }
  var newStr = str;
  for(var i =1 ;i < num ;i++){
    newStr = newStr.concat(str);
  }
  return newStr;
}

repeatStringNumTimes("abc", 3);
